import express from "express";
// import rsi from "./rsi.js";

const routerStrategy = express.Router();

// routerStrategy.all("/rsi", rsi);
// routerStrategy.use("/bb", bb);

export default routerStrategy;
